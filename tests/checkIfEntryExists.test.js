import checkIfEntryExists from '../functions/checkIfEntryExists.js';

const input = {
  1253: '1536753810370007043'
};

const id = '1536753810370007043';

test('write to db file if the entry does not exist and return false', () => {
  // expect(checkIfEntryExists(id)).toBeFalsy();
});

test('write to db file if the entry exists and return true', () => {
  // expect(checkIfEntryExists(id)).toBeFalsy();
});
